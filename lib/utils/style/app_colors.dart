import 'package:flutter/material.dart';

class AppColors {
  // 🌞 Light mode
  static const lightPrimary = Color(0xFF004799);
  static const lightPrimaryForeground = Colors.white;
  static const lightSecondary = Color(0xFFFDB813);
  static const lightSecondaryForeground = Color(0xFF1A1A1A);
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightForeground = Color(0xFF232323);
  static const lightCard = Color(0xFFF8F9FA);
  static const lightBorder = Color(0xFFE5E7EB);
  static const lightMuted = Color(0xFF9CA3AF);
  static const lightDestructive = Color(0xFFDC2626);

  // 🌙 Dark mode
  static const darkPrimary = Color(0xFF0074CC);
  static const darkPrimaryForeground = Colors.white;
  static const darkSecondary = Color(0xFFE5A100);
  static const darkSecondaryForeground = Colors.white;
  static const darkBackground = Color(0xFF1A1A1A);
  static const darkForeground = Color(0xFFEFEFEF);
  static const darkCard = Color(0xFF232323);
  static const darkBorder = Color(0xFF333333);
  static const darkMuted = Color(0xFF6B7280);
  static const darkDestructive = Color(0xFFE53935);
}
